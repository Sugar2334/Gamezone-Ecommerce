import React from 'react';
import {AiOutlineShoppingCart} from 'react-icons/ai'

export const CartWidget = () => {
    return (
        <AiOutlineShoppingCart name="cart-icon" size={23}/>
    );
}

export default CartWidget;